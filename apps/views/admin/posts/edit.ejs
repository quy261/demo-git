<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Post</title>
    <%- include("../layout/head") -%>
    <script src="//cdn.ckeditor.com/4.20.1/standard/ckeditor.js"></script>
</head>
<body>
    <%- include("../layout/nav") -%>

    <div class="container">
        <div class="col-md-6 col-md-offset-3">
            <div class="row myborder">
                <form action="" method="" id="editPostForm" role="form" enctype="application/x-www-form-urlencoded">
                    <h4 style="color: #7EB59E; margin: initial; margin-bottom: 10px;"> Edit Post </h4><hr>
                        <div class="input-group margin-bottom-20 hidden">
                            <label for="id2">Title:</label>
                            <input size="70" maxlength="255" class="form-control" placeholder="ID..." name="id" id="id2" type="text"
                            value= "<% if(data && data.post){ %> <%= data.post.id %> <% } %>">
                        </div>
                        <div class="input-group margin-bottom-20">
                            <label for="title2">Title:</label>
                            <input size="70" maxlength="255" class="form-control" placeholder="Title..." name="title" id="title2" type="text"
                            value= "<% if(data && data.post){ %> <%= data.post.title %> <% } %>">
                        </div>

                        <div class="input-group margin-bottom-20">
                            <label for="content2">Content:</label>
                            <textarea class="form-control ckeditor" placeholder="Content..." name="content" id="content2">
                                <% if(data && data.post){ %> <%= data.post.content %> <% } %>
                            </textarea>
                        </div>

                        <div class="input-group margin-bottom-20">
                            <label for="author2">Author:</label>
                            <input size="68" maxlength="255" class="form-control" placeholder="Author..." name="author" id="author2" type="text"
                            value= "<% if(data && data.post){ %> <%= data.post.author %> <% } %>">
                        </div>

                        <div class="row">
                            <div class="col-md-12 margin-bottom-20">
                                <button class="btn btn-success pull-left" type="submit" id="post_edit"> Edit </button>
                                <a href="/admin" class="btn btn-danger pull-right" role="button"> Cancel </a>
                            </div>
                        </div>
                </form> 
                <% if (data && data.error) { %>
                    <div class="error col-md-12">
                        <p> ERROR: </p>
                        <p> <%= data.error %> </p>
                    </div>
                <% } %> 
            </div>              
    </div>

    <script text="text/javascript" src="/static/js/post.js"></script>

</body>
</html>